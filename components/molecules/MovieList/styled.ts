import styled from "styled-components";

export const MovieListStyled = styled.div`
  position: relative;
  user-select: none;

  width: 100%;
  height: 100%;
  flex-wrap: wrap;

  > a {
    width: 400px;
  }
`;
